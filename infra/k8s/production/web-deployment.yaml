apiVersion: apps/v1
kind: Deployment
metadata:
  name: tlacuilo-web
  namespace: tlacuilo
  labels:
    app.kubernetes.io/name: tlacuilo-web
    app.kubernetes.io/part-of: tlacuilo
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: tlacuilo-web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: tlacuilo-web
        app.kubernetes.io/part-of: tlacuilo
        app: tlacuilo-web
        enclii.dev/service: tlacuilo-web
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      imagePullSecrets:
        - name: ghcr-credentials
      containers:
        - name: web
          # DECOY tag; the pin is in kustomization.yaml.
          image: ghcr.io/madfam-org/tlacuilo/tlacuilo-web:latest
          securityContext:
            privileged: false
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          ports:
            - name: http
              containerPort: 8080
          volumeMounts:
            - name: data
              mountPath: /data
            - name: config
              mountPath: /config
          resources:
            requests: { cpu: 10m, memory: 32Mi }
            limits: { cpu: 200m, memory: 64Mi }
          livenessProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 2
            periodSeconds: 10
      volumes:
        - name: data
          emptyDir: { sizeLimit: 16Mi }
        - name: config
          emptyDir: { sizeLimit: 16Mi }
